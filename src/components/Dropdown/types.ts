export type DropdownPositionType = {
    x: number,
    y: number,
    width: number,
    height: number,
}

export type DropdownValueType = {
    title: string;
    id: string;
    isDisabled?: boolean;
}