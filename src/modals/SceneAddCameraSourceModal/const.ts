import { createContext } from 'react';
import { SceneAddCameraSourceModalStateType } from './types';

export const SceneAddCameraSourceModalContext = createContext(undefined as unknown as SceneAddCameraSourceModalStateType);
